@model Line.Models.MiniProfileModel

<li class="nav-header">
    <div class="dropdown profile-element">
        @*<p class="text-center">
            <a href="@Url.Action("profile", "Organisers" , new { area = "admin"})"><img alt="image" class="img-circle" src="@Model.PictureUrl" width="50" /></a>

        </p>*@
        <p class=" text-center">
            <strong class="text-white">@Model.Fullname</strong>
        </p>
        @*<p class="text-muted  text-center ">@Model.Title </p>*@
        @using (Html.BeginForm("QuickSearch", "Reservations", new { Area = "admin" }))
        {
            <div class="input-group">
                <input type="text" class="form-control" id="qid" name="qid" placeholder="Search By Id">
                <div class="input-group-btn">
                    <button id="qidbtn" class="btn btn-default" type="submit">
                        <i class="glyphicon glyphicon-search"></i>
                    </button>
                </div>
            </div>
        }


        @*<ul class="dropdown-menu animated fadeInRight m-t-xs">
            <li><a href="@Url.Action("profile", "Organisers" , new { area = "admin"})">@T("admin.profile")</a></li>
            <li><a href="@Url.Action("index", "Events", new { area = "admin"})">@T("admin.events")</a></li>
            <li class="divider"></li>
            <li><a href="@Url.Action("Logout", "account", new { area = ""})">@T("admin.logout")</a></li>
        </ul>*@
    </div>
    <div class="logo-element">
        @if (string.IsNullOrWhiteSpace(Model.LogoUrl))
        {
            <span>@T("IRS")</span>
        }
        else
        {
            <img alt="image" src="@Model.LogoUrl" width="60" />
        }
    </div>
</li>
